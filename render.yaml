---
# Render Blueprint Specification for ECU Men's Ice Hockey Calendar Service
# https://render.com/docs/blueprint-spec
services:
  # Web Service: FastAPI Calendar & Data API
  - type: web
    name: ecu-hockey-api
    runtime: docker
    plan: starter
    region: oregon
    dockerfilePath: ./Dockerfile
    preDeployCommand: alembic upgrade head
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ecu-hockey-db
          property: connectionString
      - key: ADMIN_API_TOKEN
        generateValue: true
      - key: AUTO_MIGRATE
        value: 'true'
      - key: LOG_LEVEL
        value: info
      - key: DISCORD_WEBHOOK_URL
        sync: false
      - key: SLACK_WEBHOOK_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false

  # Scheduled Cron Worker: Multi-Source Schedule Synchronization & Change Detection
  - type: cron
    name: ecu-hockey-worker
    runtime: docker
    plan: starter
    region: oregon
    schedule: 0 */6 * * *
    dockerfilePath: ./Dockerfile
    dockerCommand: ecu-hockey sync --notify
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ecu-hockey-db
          property: connectionString
      - key: ADMIN_API_TOKEN
        fromService:
          type: web
          name: ecu-hockey-api
          envVarKey: ADMIN_API_TOKEN
      - key: DISCORD_WEBHOOK_URL
        sync: false
      - key: SLACK_WEBHOOK_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
databases:
  - name: ecu-hockey-db
    plan: starter
    region: oregon
    databaseName: ecu_hockey
    user: ecu_hockey_user
